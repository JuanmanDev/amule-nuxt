<!--
Commit subjects follow Conventional Commits (feat:, fix:, docs:, chore:...):
semantic-release derives the version and the changelog from them, so a
"fixed stuff" costs a release. `npm run lint:commits` checks yours.
-->

## What and why

## How it was checked

- [ ] `npm run typecheck` and `npm test` pass
- [ ] Tried against a real daemon (which version?) — or explain why not needed
- [ ] New strings added to `i18n/locales/en.json` (other languages fall back to English)
